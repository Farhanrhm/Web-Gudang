<?php

// Placeholder export class kept for compatibility. The project
// currently uses the maatwebsite/excel v1 create()->sheet() API
// directly from the controller, so this file is intentionally
// left inert to avoid autoload issues with newer interfaces.

namespace App\Exports;

class ProductsExport
{
    // Intentionally empty placeholder.
}

